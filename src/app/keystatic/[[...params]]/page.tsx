"use client"

import { makePage } from "@keystatic/next/ui/app"
import config from "../../../../keystatic.config"

/** Interface d'administration, servie à /keystatic. */
export default makePage(config)
